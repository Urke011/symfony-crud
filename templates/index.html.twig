
  {# {{dump(title)}} #}  
   


{#
{% if title %}
<p>{{title }} is a movie!</p>

{% else%}
<p>No title has been set</p>
{% endif %}
#}

{% extends 'base.html.twig' %}
{#
{% block title %}
    Movies Page
{% endblock %}

{% block body %}
    <h1>{{title}}</h1>    
{% endblock %}
#}

{% block body %}
    {% for movie in movies %}
        <li>{{movie}}</li>
        {# globals variables#}
     
    {% endfor %}
    <h3>{{ author }}</h3>
    <h4>{{_self}}</h4>
{% endblock %}
  